spring:
  application:
    name: dept-server
  profile: prod

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka
  instance:
    instance-id: service-provider-${server.port}
    prefer-ip-address: true
